```dataviewjs
let folderFiles = dv.pages('"000-箱的构成/箱中金句"').file;
let fileContents = await Promise.all(folderFiles.map(file => app.vault.readRaw(file.path)));
let sentenceList = fileContents.map(content => content.split("\n"));
let ninthLine = sentenceList[9];
let oneSentence = sentenceList[ninthLine.length * Math.random() | 0]
dv.paragraph(`${oneSentence}$`)
```